{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -qE '(^|[;&|(])[[:space:]]*gh[[:space:]]+pr[[:space:]]+(create|new)([[:space:]]|$)' && printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Hook projektu: najpierw uruchom /review-pr, napraw znalezione problemy, i dopiero potem twórz PR. Zobacz CLAUDE.md, sekcja Przed otwarciem PR.\"}}' || true",
            "statusMessage": "Sprawdzam, czy PR przeszedł review"
          }
        ]
      }
    ]
  }
}
